import { CasesPage } from '@/components/marketing/EnglishPages';
import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata({
  title: 'AI Agents, Bots & Platform Projects — Rhema AI',
  description: 'Explore Rhema AI projects: AI agents, Telegram Mini Apps, bots, platforms, multi-currency tracking and city reporting, with real product screens and solutions.',
  path: '/cases',
  locale: 'en',
});

export default CasesPage;
