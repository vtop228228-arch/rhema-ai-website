import { WorkPage } from '@/components/marketing/EnglishPages';
import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata({
  title: 'AI Implementation Process & Pricing — Rhema AI',
  description: 'From workflow discovery to testing and launch. See how Rhema AI defines project scope, builds AI agents and platforms, and supports your team.',
  path: '/how-we-work',
  locale: 'en',
});

export default WorkPage;
