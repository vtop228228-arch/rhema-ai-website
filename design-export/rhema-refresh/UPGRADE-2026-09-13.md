# Website upgrade — 13 September 2026

## Delivered

- Follow-up: all four task previews now use explicitly labelled three-step workflows instead of unrelated photos or cropped screenshots. Case links explain the project type. Verified 48 RU/EN viewport/selection states and four accessibility audits.

- Home and services task selector: websites/apps, customer replies, recurring operations, content. Each selection includes scope, a service link and a real project example, in RU/EN.
- Service summary: existing price/timing estimates and inputs needed from the client. No new contractual claims, testimonials or performance numbers.
- Service case links now include screenshots and project descriptions.
- Header contact CTA stays on the current case/service page. Legal pages and demo retain the homepage contact fallback.
- Fixed a decorative orbit overflowing the homepage at narrow widths.
- Analytics waits for opt-in. Decline persists across reloads. No session recording. Explicit pageviews support client-side navigation without duplicate initial hits.
- Added goals: `task_selected`, `case_view`, `service_view`, `telegram_click`, `contact_click`, `contact_start`. Existing `contact_lead` remains success-only. Goal parameters contain task/page/project identifiers, not form contents.

## Verification

- Production build: 48 generated pages/routes; TypeScript and scoped ESLint passed.
- 36 responsive scenarios (320, 390, 1440 pixels; RU/EN), 12 axe audits.
- All task options and target links; contact CTA remains on the case page.
- Mocked HTTP 500 then HTTP 200: data retained on failure, success after retry. No real leads submitted.
- Mocked analytics transport: no load before consent or after decline/reload; one transport load after acceptance; one pageview per navigation; task and case goals verified.
- Results: `ux-upgrade-checks.json`; screenshots: `upgrade-*.png`.

## Analytics account follow-up

In the existing Yandex Metrica counter, add JavaScript-event goals with the identifiers above if they do not already exist. Account-side goal configuration and report visibility have not been verified: this workspace has no authenticated Metrica dashboard access. Implementation reference: https://yandex.com/support/metrica/en/objects/reachgoal and https://yandex.com/support/metrica/en/objects/hit.

## Evidence still needed from owners

Client-approved reviews, measured before/after results and actual project delivery durations can strengthen cases later. None have been fabricated.
